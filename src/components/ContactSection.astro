---
import ContactIcon from "../assets/icons/ContactIcon.astro";
import CopyIcon from "../assets/icons/CopyIcon.astro";
import SendIcon from "../assets/icons/SendIcon.astro";
---

<section id="contact" class="w-full flex flex-col gap-4 px-8 pt-20">
    <h2 class="flex items-center gap-2 text-3xl font-bold opacity-95">
        <ContactIcon class="size-6" />
        Contacto
    </h2>
    <div class="flex flex-col gap-4 text-lg text-gray-300 text-pretty">
        <p> Si quieres contactarme, puedes enviarme un correo electrónico a: </p>
        
        <span class="flex items-center justify-between text-base border border-gray-800 rounded-full transition pl-4 py-1 hover:border-gray-700 sm:text-lg">
            axel.larrea.9@gmail.com
            <div class="flex xs:gap-2">
                <button 
                    class="flex items-center py-2 pr-4 cursor-pointer"
                    aria-label="Copy email" 
                    onclick="navigator.clipboard.writeText('axel.larrea.9@gmail.com')"
                    title="Copy email"
                >
                    <CopyIcon class="size-6" />
                </button>

                <button 
                    class="flex items-center py-2 pr-4 cursor-pointer"
                    aria-label="Send email" 
                >
                    <a href="mailto:axel.larrea.9@gmail.com" aria-label="Send email" >
                        <SendIcon class="size-6" />
                    </a>
                </button>
            </div>
        </span>
    </div>
</section>